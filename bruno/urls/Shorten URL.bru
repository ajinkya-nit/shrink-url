meta {
  name: "Shorten URL"
  type: "http"
  seq: 1
}

post {
  url: "{{baseUrl}}/api/url/shortenurl"
  body: "json"
  auth: bearer
}

auth:bearer {
  token: "{{accessToken}}"
}

body:json {
  {
    "originalUrl": "https://example.com",
    "isActive": true,
    "expiry": "1d"
  }
}
​