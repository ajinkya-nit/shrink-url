meta {
  name: "Active URLs"
  type: "http"
  seq: 2
}

get {
  url: "{{baseUrl}}/api/dashboard/activeurls?page=1&limit=10&sortType=desc"
  body: "none"
  auth: bearer
}

auth:bearer {
  token: "{{accessToken}}"
}
​