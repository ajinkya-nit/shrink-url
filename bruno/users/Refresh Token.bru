meta {
  name: "Refresh Token"
  type: "http"
  seq: 3
}

post {
  url: "{{baseUrl}}/api/users/refresh-token"
  body: "json"
  auth: none
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}
​